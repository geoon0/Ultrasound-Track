# Ultra_Track 실시간 추론/시각화 실행에 필요한 패키지
# (Windows base 환경 기준 버전 — macOS 에서도 동일 버전대로 동작)
#
# 설치:  pip install -r requirements.txt
# 권장 Python: 3.11 ~ 3.13
#
# Apple Silicon(M1~) Mac 참고:
#   - 아래 tensorflow 가 arm64 휠로 그대로 설치됩니다(별도 tensorflow-macos 불필요).
#   - 모델이 작아 CPU로 충분하므로 tensorflow-metal(GPU) 은 설치하지 않아도 됩니다.

tensorflow>=2.18,<2.22   # keras 3 번들 포함 (.keras 모델 로드에 필요)
numpy>=1.26
pyserial>=3.5
matplotlib>=3.8
